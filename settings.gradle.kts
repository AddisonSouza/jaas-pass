pluginManagement {
    repositories {
        google {
            content {
                includeGroupByRegex("com\\.android.*")
                includeGroupByRegex("com\\.google.*")
                includeGroupByRegex("androidx.*")
            }
        }
        mavenCentral()
        gradlePluginPortal()
    }
}

// Repositórios usados APENAS para resolver o plugin Android/Kotlin essencial.
// Nenhuma dependência de terceiros entra no app (ver app/build.gradle.kts).
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "jaas-pass"
include(":app")
include(":vault-crypto")
